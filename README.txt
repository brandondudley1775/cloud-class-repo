1. This web server requires Flask, make sure to install it.
2. If you are on a Windows machine, you can just start the server with the included batch file.
3. If not on Windows, start the server the same way you start a Flask server on Linux.  Python file is main.py.
4. Run postman tests on localhost.
